# Telegram Bot - Latent Space FM

## Форматирование сообщений

Все посты бота должны использовать HTML форматирование:
- При отправке сообщений указывать `parse_mode="HTML"`
- Использовать теги: `<b>`, `<i>`, `<code>`, `<pre>`, `<a href="">`, `<s>`, `<u>`
- Избегать Markdown (`*`, `_`, ``` и т.д.)
